# 환경변수 — 절대 커밋 금지 (API 키 포함)
.env
.env.local
.env.*.local

# 의존성
node_modules/

# 빌드 산출물
apps/web/.next/
apps/mobile/.expo/
dist/
build/

# 로그·OS
*.log
.DS_Store

# 로컬 저장소(JSON 파일 DB) — 런타임 데이터
server/.data/
